Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-JWT-Extended==4.6.0
Flask-Cors==4.0.1

# Vendor-neutral application metrics (CHECKLIST.md Phase 7B) -- the
# Prometheus text exposition format (GET /metrics) is a de facto open
# standard scraped by Prometheus, Grafana Agent, Datadog, and most other
# observability vendors alike; nothing here is tied to any one of them.
# Pure Python, no external service required to just collect metrics
# in-process.
prometheus-client==0.21.1

# OIDC access-token verification (app/security/jwt_verifier.py) --
# standard issuer/JWKS/audience/expiry checks against Keycloak locally
# (any OIDC-compliant IdP in production, see docs/local-development.md).
# The [crypto] extra pulls in `cryptography`, needed for RS256.
PyJWT[crypto]==2.14.0
psycopg2-binary==2.9.9
gunicorn==22.0.0
python-dotenv==1.0.1

# Transactional outbox publisher (app/services/outbox_service.py) — only
# ever touched by `flask events publish-outbox`, never imported at
# request-handling time, but it's a small, dependency-light pure-Python
# package so there's no reason to make it a lazy/optional import the way
# the ML runtime libraries below are. kafka-python-ng, not kafka-python:
# the original kafka-python==2.0.2 ships a broken vendored `six` under
# Python 3.12 (ModuleNotFoundError: kafka.vendor.six.moves) -- this is the
# actively maintained fork, same `import kafka` API, drop-in compatible.
kafka-python-ng==2.2.3

# Real model runtime (apps/api/app/services/runtimes/) — loads a trained
# artifact produced by apps/workers/ml (see docs/model_cards/). Training
# and serving stay decoupled: this only ever joblib.loads an artifact and
# reads feature metadata already stored on ModelVersion, never imports
# apps/workers code.
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.0
xgboost==2.0.3
shap==0.45.1
joblib==1.4.2

pytest==8.2.2
pytest-cov==5.0.0
ruff==0.5.0
