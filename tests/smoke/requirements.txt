# Deliberately separate from apps/api/requirements.txt: this suite is a
# black-box HTTP client for a *deployed* environment, not part of the
# Flask app itself -- it has no business sharing that app's dependency
# tree (SQLAlchemy, gunicorn, the ML runtime libraries, ...).
requests==2.32.3
pytest==8.2.2
